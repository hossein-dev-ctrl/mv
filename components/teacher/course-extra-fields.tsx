import {deliveryLabels} from '@/components/course/delivery-status';
export default function CourseExtraFields({status='ONGOING',intro=''}:{status?:string;intro?:string|null}) {
 return <section className="space-y-5 rounded-2xl border bg-white p-6"><h2 className="font-bold">برگزاری و معرفی مدرس</h2><label className="block text-sm">وضعیت برگزاری<select name="deliveryStatus" defaultValue={status} className="mt-2 block w-full rounded-xl border p-3">{Object.entries(deliveryLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><p className="text-xs leading-7 text-slate-500">در حالت «به‌زودی» فقط علاقه‌مندی و اطلاعات تماس ثبت می‌شود؛ خرید فعال نیست. برای نمایش عمومی، وضعیت انتشار دوره را هم منتشرشده کنید. پایان‌یافته یعنی محتوای دوره تکمیل شده و همچنان قابل خرید است.</p><label className="block text-sm">دربارهٔ مدرس<textarea name="teacherIntro" defaultValue={intro??''} maxLength={3000} rows={4} className="mt-2 block w-full rounded-xl border p-3" placeholder="تخصص، تجربه و شیوهٔ تدریس مدرس این دوره"/></label></section>;
}
